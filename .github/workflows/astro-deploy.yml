name: Deploy Astro Blog

on:
  push:
    branches: ["main"]
    paths:
      - 'frameworks/astro-blog/**'
      - '.github/workflows/astro-deploy.yml'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: 'npm'
          cache-dependency-path: frameworks/astro-blog/package-lock.json

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Install dependencies
        run: npm ci
        working-directory: frameworks/astro-blog

      - name: Build with Astro
        run: npm run build
        working-directory: frameworks/astro-blog

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: frameworks/astro-blog/dist

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
